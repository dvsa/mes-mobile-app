export var initialState = {
    dangerousFaultComments: [],
    drivingFaultComments: [],
    seriousFaultComments: [],
};
export function officeReducer(state, action) {
    if (state === void 0) { state = initialState; }
    switch (action.type) {
        default:
            return state;
    }
}
//# sourceMappingURL=office.reducer.js.map