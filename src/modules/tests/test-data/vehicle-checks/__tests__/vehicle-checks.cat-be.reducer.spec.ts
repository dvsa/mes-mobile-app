// import {VehicleChecksCatBeActionTypes} from '../vehicle-checks.cat-be.action';

// import {
//   initialState,
//   vehicleChecksCatBEReducer,
// } from '../vehicle-checks.cat-be.reducer';

// describe('Vehicle Checks Cat B+E Reducer', () => {
//   it('should return the initial state', () => {
//     const result = vehicleChecksCatBEReducer(initialState, {type: VehicleChecksCatBeActionTypes});
//     expect(result).toBe(initialState);
//   });
// });
