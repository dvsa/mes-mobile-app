import { FaultSummary } from '../models/fault-marking.model';

export const getSingleFaultCompetencyDrivingFaults = (): FaultSummary[] => {
  const faultsEncountered: FaultSummary[] = [];

  return faultsEncountered;
};
